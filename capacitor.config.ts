import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ng.greenvillemontessorischools.portal',
  appName: 'Greenville Montessori',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
